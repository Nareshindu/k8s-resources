apiVersion: v1
kind: Pod
metadata:
  name: annotations
  annotations:
    imageregistry: "https://hub.docker.com/"
spec:
  containers:
    name: postgres
    image: postgres
    ports:
    - containerPort: 5432    